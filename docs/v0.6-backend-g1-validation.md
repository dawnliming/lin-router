# v0.6 后端 G1：验收记录

- 验收范围：I1 执行平面无行为重构
- 回滚基线：`ea534bc`（v0.5.6 发布元数据）
- `frontend/`：不属于本后端变更集，禁止进入后端提交

## `/health` smoke 归因

`/health` smoke 未通过，真实响应为 HTTP 404。该结果是 **基线既存缺陷**，不是 I1 回归：

1. `linrouter_core/runtime/http_api_runtime.py` 在当前工作树与 `ea534bc` 完全一致；
2. 两个版本均先以静态资源兜底处理非 `/api/`、非 `/v1/` 的路径，后续 `/health` 分支不可达；
3. 隔离 `create_server` 的真实 HTTP 请求复现 `/health` 为 404。

按无行为重构边界，本次不调整路由顺序，也不将该 smoke 计为通过。后续应单独立项修复 `/health` 路由顺序，并补真实 HTTP smoke 回归。

## G1 验收结论

- I1 执行平面范围内的自动化、专项、源码边界和 Codex 复核按已有记录通过；
- `/health` 不作为本次 G1 的通过证据，登记为基线 smoke 缺口；
- 因存在该未修复基线缺口，G1 **不宣布全绿通过**；后端 I1 变更仍可按独立提交分组，回滚至 `ea534bc`。
